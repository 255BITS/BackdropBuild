<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {% for path, data in paths.items() %}
    <url>
        <loc>{{ path }}</loc>
        <lastmod>{{ data.last_updated.strftime('%Y-%m-%dT%H:%M:%S%z') }}</lastmod>
    </url>
    {% endfor %}
</urlset>
